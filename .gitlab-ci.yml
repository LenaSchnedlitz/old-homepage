image: cypress/base:12.16.1

stages:
  - test
  - deploy


test:
  stage: test
  script:
    - npm run test

deploy:
  stage: deploy
  script:
    - npm run test
    - npm run export
    - mv __sapper__/export public
  artifacts:
    paths:
      - public
  only:
    - master